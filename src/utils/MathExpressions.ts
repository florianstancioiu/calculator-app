export const deleteExpression = (text: string) => {
  return text;
};

export const addExpression = (text: string) => {
  return text;
};

export const subtractExpression = (text: string) => {
  return text;
};

export const multiplyExpression = (text: string) => {
  return text;
};

export const divideExpression = (text: string) => {
  return text;
};

export const resetExpression = (_text: string) => {
  return "";
};

export const equalsExpression = (_text: string) => {
  return "";
};
